{% extends 'base.html.twig' %}

{% block title %}Ajouter une salle de réunion{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-5 my-5 mx-auto">
                <div id="first">
                    <div class="myform form">
                        <div class="logo mb-3">
                            <div class="col-md-12 text-center">
                                <h1>Ajouter une salle de réunion</h1>
                                {{ form_start(form) }}
                                {% if not form.vars.valid %}
                                    <div class="alert alert-danger">
                                        {% for child in form.children %}
                                            {% for error in child.vars.errors %}
                                                {{ error.message }} <br>
                                            {% endfor %}
                                        {% endfor %}
                                    </div>
                                {% endif %}
                            </div>
                        </div>


                        <div class="form-group">
                            {{form_label(form.name)}}
                            {{form_widget(form.name)}}
                        </div>

                        <div class="form-group">
                            {{form_label(form.seats)}}
                            {{form_widget(form.seats)}}
                        </div>

                        <div class="form-group">
                            {{form_label(form.hasProjector)}}
                            {{form_widget(form.hasProjector)}}
                        </div>

                        <div class="col-md-12 text-center my-3">
                            <button type="submit" class=" btn btn-block mybtn btn-primary tx-tfm">Ajouter</button>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
